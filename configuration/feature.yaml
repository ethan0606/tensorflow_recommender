label:
  label_name: label
  dtype: float
numeric:

category:
  adgroup_id_encode:
    voc_dim: 50000
  cate_id:
    voc_dim: 13000
  price:
    voc_dim: 5
  final_gender_code:
    voc_dim: 2
  age_level:
    voc_dim: 7
  pvalue_level:
    voc_dim: 4
  shopping_level:
    voc_dim: 4
  occupation:
    voc_dim: 2
  new_user_class_level:
    voc_dim: 5

sparse_category:
  user_interest:
    voc_dim: 50000
    max_dim: 100
    embedding_name: adgroup_id_encode